# 個人專屬富文本筆記本軟體 (Custom Notebook App)

打造一個功能全面、介面精美且支援多功能編輯、圖片插入、全文搜尋以及 **匯出為 Word (.docx)** 的個人筆記本應用程式。

## 🎯 核心功能亮點

1. **富文本編輯器 (Rich Text Editor)**
   - **字體與顏色控制**：可自訂字體大小、文字顏色、背景螢光筆顏色。
   - **格式排版**：粗體、斜體、底線、刪除線、標題 (H1-H3)、引用區塊、程式碼區塊。
   - **清單與待辦事項**：項目符號清單、數字清單、可勾選待辦清單 (Checklist)。
   - **圖片插入與管理**：支援拖曳插入圖片、複製貼上圖片、調整圖片大小與對齊。

2. **📄 Word 檔 (.docx) 匯出**
   - 支援將筆記（包含文字格式、標題、樣式與圖片）一鍵打包匯出為真正的 Microsoft Word (`.docx`) 檔案。
   - 同時支援匯出為 PDF, Markdown, 以及備份檔 (JSON)。

3. **🔍 快速搜尋與分類**
   - 全文即時搜尋（標題與內文關鍵字高亮）。
   - 分類標籤 (Tags) 與資料夾夾層管理。
   - 支援筆記固定 (Pin) 與按修改時間排序。

4. **💾 本地資料安全與自動儲存**
   - 編輯過程離線自動儲存（使用 IndexedDB，可儲存大量圖片與文字數據）。
   - 支援資料匯入/匯出與備份，隱私完全屬於使用者本地端。

5. **✨ 現代視覺美學 (Modern Visual Design)**
   - 支援深色模式 (Dark Mode) 與淺色模式 (Light Mode) 一鍵切換。
   - 側邊欄折疊、響應式排版、極致流暢的微動畫與視覺體驗。

---

## 🛠️ 技術架構選型

- **前端框架/技術**：HTML5, Modern CSS (Vanilla CSS 變數系統 + 質感設計), Vanilla JavaScript (ES6 Modules)
- **編輯器核心**：Quill.js / Tiptap 輕量高級富文本引擎
- **Word 匯出引擎**：`docx.js` / `html-docx-js-typescript` (將編輯器 HTML 與圖片轉碼打包為原生 `.docx` 文件)
- **本地儲存引擎**：`Dexie.js` (IndexedDB 封裝，高效處理包含大圖的筆記資料)

---

## 📋 實作步驟計劃

### 1. 基礎架構與樣式系統 (`index.html`, `styles.css`)
- 設計極簡質感的 UI 佈局（側邊欄 + 工具列 + 編輯主區塊 + 狀態欄）。
- 配置 CSS 變數（主題顏色、字體尺寸、陰影與圓角）。

### 2. 編輯器與工具列整合 (`js/editor.js`)
- 整合字體大小下拉選單、調色盤（文字顏色/背景顏色）。
- 實作圖片上傳、拖拽與調整大小機制。

### 3. Word (.docx) 匯出模組 (`js/export.js`)
- 整合 Word 打包工具，將筆記內容（HTML + Image Data URI）轉碼轉換為標準 `.docx` 二進位檔供瀏覽器下載。

### 4. 筆記管理與搜尋系統 (`js/storage.js`, `js/app.js`)
- 實作 IndexedDB 資料儲存與筆記CRUD（新增、讀取、更新、刪除）。
- 實作側邊欄筆記清單與即時全文搜尋高亮。

---

## 🧪 驗證計畫

### 功能測試：
1. **編輯器功能驗證**：測試改變字體大小、顏色、粗斜體、插入圖片是否正常顯示。
2. **Word 匯出驗證**：建立包含標題、各式顏色文字、列表及圖片的筆記，點擊「匯出 Word」，使用 MS Word 或 WPS 開啟導出的 `.docx` 檔案，驗證文字格式與圖片是否完整保留。
3. **搜尋驗證**：搜尋關鍵字確認筆記清單即時篩選正確。
4. **自動儲存驗證**：關閉並重新開啟瀏覽器頁面，確認筆記內容完整無失。
