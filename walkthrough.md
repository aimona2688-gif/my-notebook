# Aura Note - 尊榮個人筆記本軟體 (完成紀錄)

我們已為您成功構建專屬於您的高質感筆記本應用程式 **Aura Note**。

## 🌟 核心功能說明

### 1. 🖼️ 本地圖片插入與拖拽功能
- **點擊插入**：點擊編輯器工具列上的圖片圖示即可選擇本地電腦圖片插入筆記中。
- **拖拽插入**：可直接將電腦資料夾中的圖片拖拽至網頁編輯區內放開，自動插入圖片。
- **自適應排版**：圖片具備縮放能力，並會在匯出 Word 時一併打包儲存。

### 2. 🎨 豐富文字與格式工具
- **字體大小與樣式**：選取文字後可隨心切換小字體、正常字體、大字體、超大字體，或是 H1~H3 標題結構。
- **文字顏色與調色盤**：自由調整字體顏色與螢光筆畫線背景。
- **清單與待辦事項**：支援項目符號列表、編號列表、勾選式待辦清單 (Checklist)。

### 3. 📄 Word (.docx) 一鍵匯出
- 點擊右上角「**匯出檔案**」選單中的「**匯出為 Word (.docx)**」，系統會將筆記內容（包含所有格式、顏色、列表與插入的圖片）自動轉換打包為原生 `.docx` 文件，方便在 MS Word 或 WPS 中編輯與傳閱。
- 同時支援匯出 HTML, 純文字 (.txt) 與 JSON 資料備份。

### 4. 🔍 全文即時搜尋與標籤
- 支援於左側邊欄搜尋框中即時全文檢索標題與內文關鍵字。
- 支援筆記釘選 (Pin) 與標籤 (#tag) 分類。

### 5. 🔒 本地 IndexedDB 離線安全防護
- 採用瀏覽器 IndexedDB 數據庫技術，筆記編輯過程中自動離線儲存，無須擔心斷網或忘記存檔，資料 100% 存在您的電腦本機端。

---

## 🚀 如何使用 Aura Note？

1. 您隨時可以用瀏覽器開啟此專案資料夾下的 [`index.html`](file:///C:/Users/student/.gemini/antigravity/brain/da96fe42-98c1-40d5-ba92-44a1b7cdc5c7/index.html)。
2. 或在瀏覽器中開啟：`http://localhost:8080` (本地預覽伺服器已為您啟動)。
